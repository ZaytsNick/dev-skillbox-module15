#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> a = {-2, 1, -3, 4, -1, 2, 1, -5, 4};
    int index_start, index_end;
    for (int i = 0; i < a.size(); ++i)
    {
        if (a[i] >a[i+1])
        {
            for (int j = i; j < a.size(); ++j)
            {
                
            }
        }
    }
}